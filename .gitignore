# deps & build
node_modules/
.next/
dist/
# env & secrets
.env
.env.local
*.env
.env.*
!apps/api/.env.example
# os / ide
.DS_Store
Thumbs.db
.idea/
.vscode/
# logs
logs/
*.log
# uploaded user content
apps/api/uploads/*
!apps/api/uploads/.gitkeep
# QA / tooling artefacts (not product code)
docs/media/qa-screenshots/
docs/image-assets/catalogue-products.csv
# One-off build/capture/debug scripts, not part of the app. capture_golden_path.js and
# verify_fixes.js are already tracked in git — a tracked file being under a later ignore
# pattern doesn't untrack it, this just stops *new* one-off scripts from being added here.
scripts/dev-tools/
# session scratch space (rendering sources, temp API/DB test artefacts)
tmp/
docs-panel-core/
